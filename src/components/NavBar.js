import React, { Component } from 'react';

class NavBar extends Component {
  render() {
    return (
      <nav id="navbar">
        <h2 id="head-text">Neighborhood Maps</h2>
      </nav>
    );
  }
}

export default NavBar;
